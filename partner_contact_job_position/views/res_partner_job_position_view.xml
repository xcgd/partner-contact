<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="res_partner_job_position_action" model="ir.actions.act_window">
        <field name="name">Job Positions</field>
        <field name="res_model">res.partner.job_position</field>
        <field name="view_mode">tree,form</field>
    </record>

    <record id="res_partner_job_position_search_view" model="ir.ui.view">
        <field name="name">Job position search</field>
        <field name="model">res.partner.job_position</field>
        <field name="arch" type="xml">

            <search string="Job position search">
                <field name="name"/>
                <field name="code"/>
                <filter name="active" string="Active"
                    domain="[('active','=',True)]"
                    help="Show active job positions"/>
                <filter name="inactive" string="Inactive"
                    domain="[('active','=',False)]"
                    help="Show inactive job positions"/>
            </search>

        </field>
    </record>

    <record id="res_partner_job_position_tree_view" model="ir.ui.view">
        <field name="name">Job position tree</field>
        <field name="model">res.partner.job_position</field>
        <field name="arch" type="xml">

            <tree string="Job positions" editable="top"
                default_order="code asc"
                decoration-muted="(not active)">
                <field name="code"/>
                <field name="name"/>
                <field name="active" widget="boolean_toggle"/>
            </tree>

        </field>
    </record>

    <record id="res_partner_job_position_form_view" model="ir.ui.view">
        <field name="name">Job position form</field>
        <field name="model">res.partner.job_position</field>
        <field name="arch" type="xml">

            <form string="Job positions">
                <group col="4">
                    <field name="name"/>
                    <field name="active" widget="boolean_toggle"/>
                    <field name="code"/>
                </group>
            </form>

        </field>
    </record>

    <menuitem
        action="res_partner_job_position_action"
        id="menu_res_partner_job_position"
        parent="contacts.res_partner_menu_config"
        sequence="4"
    />
</odoo>
